# Environment variables
.env

# IDE settings
.idea/

# Python cache
__pycache__/
*.pyc
.zshrc

# Database (data never belongs in the repo — code only)
data/
*.db
*.db-wal
*.db-shm

# Documents (root-only, so templates/*.html are NOT ignored)
/*.docx
/*.html
